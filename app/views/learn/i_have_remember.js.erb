<% if !$items.blank? %>
  $.ajax({
  async:true,
  dataType:'script',
  url:"/learn/task_dispatch?category=<%= $category %>",
  type:'get'
  })
<% elsif $items.blank? %>
  clearInterval(myTime.timeId);
  $("#pass").show();
<% end %>